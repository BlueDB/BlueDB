ext {
	mockitoVersion = '1.9.0'
}

repositories {
	mavenCentral()
}

dependencies {
	compile project(":BlueDB")
	compile project(":BlueDBOnDisk")
	testImplementation group: 'org.mockito', name: 'mockito-all', version: mockitoVersion
	testImplementation "junit:junit:4.12"
}